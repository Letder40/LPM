use std::fs::File;
use std::path::{Path, Pathbuf};

fn main() {
    let bin: String = String::from("../../binaries/windows/lpm_win");
    let bin: String = String::from("../../binaries/windows/lpm_win");
}
